# Backend (Express + Drizzle + PostgreSQL)

Project documentation: **[../docs/README.md](../docs/README.md)**

```bash
npm install
npm run dev          # :3001
npm test
npm run docs:api     # regenerates ../docs/API.md
```

Configuration: `.env.example` → `.env`. Mounted routes: `src/app.ts`, OpenAPI catalog: `src/openapi/routeCatalog.ts`.
