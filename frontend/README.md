# Frontend (React + Vite + Tailwind)

Project documentation: **[../docs/README.md](../docs/README.md)**

```bash
npm install
npm run dev    # http://localhost:5175
npm run build
```

Default API: `http://localhost:3001` (`VITE_API_URL` / proxy in Docker).
